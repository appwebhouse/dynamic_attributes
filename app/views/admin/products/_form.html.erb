<%= simple_form_for resource, url: url, method: method do |f| %>
  <%= f.input :name %>

  <%= f.input :category do %>
    <%= f.input :category_id, collection: Category.all, label_method: :name, value_method: :id, label: false %>
  <% end %>

  <%= yield f %>
<% end %>
